[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rss-to-html"
version = "0.1"
license = "CC0-1.0"
license-files = ["LICENSE"]
dependencies = [
    "airium",
    "atoma"
]

[project.scripts]
rss-to-html = ["rss-to-html:main"]
